[Unit]
Description=Ambulance Admin Frontend
After=network.target

[Service]
Type=simple
User=ambulance
Group=ambulance
WorkingDirectory=/opt/ambulance-system/frontend/admin
ExecStart=/usr/bin/npx serve -s dist -l 5176
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
